version: '3.8'
services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_DATABASE: testdb
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"

  spring:
    image: alae_yakoubi_3c_sde1_springapp
    ports:
      - "8080:8080"
    depends_on:
      - mysql
